<p id="notice"><%= notice %>

<table>
  <tr>
    <td><strong>Profile Image</strong><td>
    <td><%= image_tag @user.image.url %>
  </tr>
</table>

<table>
  <strong>Name:</strong>
  <%= @user.name %>
</table>

<%= link_to "Edit", edit_user_path(@user.id) %>